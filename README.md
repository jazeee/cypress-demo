# Cypress Demo for presentation

### Install
```
yarn install
```

### Run
```
# Run Specs using GUI
yarn cypress:open
# Run all specs in headless env.
yarn cypress:run
# Run a single spec:
yarn cypress:run --spec cypress/integration/1-tests/google.spec.js
```
